CREATE TABLE [dbo].[DaftarMenu] (
    [no_makanan]   INT           NOT NULL,
    [Nama_Makanan] VARCHAR (MAX) NULL,
    [Harga]        INT           NULL,
    PRIMARY KEY CLUSTERED ([no_makanan] ASC)
);



